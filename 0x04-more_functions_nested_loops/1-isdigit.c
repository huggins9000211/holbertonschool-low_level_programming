/* printsa the alphabe */
#include "holberton.h"
/**
 * _isdigit - Prints a string
 * @c: int
 *
 * Return: int
 */
int _isdigit(int c)
{
	if (c <= 9 && c >= 0)
	{
		return (1);
	}
	else
	{
		return (0);
	}
	return;
}
