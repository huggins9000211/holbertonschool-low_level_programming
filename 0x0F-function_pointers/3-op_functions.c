#include "3-calc.h"
#include <stdio.h>
#include <stdlib.h>
/**
 * op_add - check if the absolute value of a number is 98
 * @a: the integer to check
 * @b: the integer to check
 *
 * Return: 0 if false, something else otherwise.
 */
int op_add(int a, int b)
{
	return (a + b);
}
/**
 * op_sub - check if the absolute value of a number is 98
 * @a: the integer to check
 * @b: the integer to check
 *
 * Return: 0 if false, something else otherwise.
 */
int op_sub(int a, int b)
{
	return (a - b);
}
/**
 * op_mul - check if the absolute value of a number is 98
 * @a: the integer to check
 * @b: the integer to check
 *
 * Return: 0 if false, something else otherwise.
 */
int op_mul(int a, int b)
{
	return (a * b);
}
/**
 * op_div - check if the absolute value of a number is 98
 * @a: the integer to check
 * @b: the integer to check
 *
 * Return: 0 if false, something else otherwise.
 */
int op_div(int a, int b)
{
	return (a / b);
}
/**
 * op_mod - check if the absolute value of a number is 98
 * @a: the integer to check
 * @b: the integer to check
 *
 * Return: 0 if false, something else otherwise.
 */
int op_mod(int a, int b)
{
	return (a % b);
}
